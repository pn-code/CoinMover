import "@nomiclabs/hardhat-ethers";
import "@nomiclabs/hardhat-waffle";

const config = {
  solidity: "0.8.18",
  networks: {
    sepolia: {
      url: "",
      accounts: []
    }
  }
};

export default config;
